{
  "name": "@flux-player/utils",
  "version": "0.0.1",
  "description": "Flux Player Core Components - Utilities",
  "main": "dist/index.js",
  "repository": "https://github.com/flux-player/flux-utils.git",
  "author": "Melvin Mupondori",
  "license": "MIT",
  "types": "dist/index.d.ts",
  "prepublish": "build",
  "scripts": {
    "build": "tsc --build tsconfig.json",
    "test": "jest --forceExit --coverage --verbose",
    "lint": "tsc --noEmit && eslint \"**/*.{js,ts}\" --quiet --fix",
    "watch-test": "npm run test -- --watchAll",
    "watch": "tsc -W"
  },
  "publishConfig": { "registry": "https://npm.pkg.github.com/" },
  "devDependencies": {
    "@types/jest": "^25.1.4",
    "@types/node": "^13.9.2",
    "jest": "^24.9.0",
    "ts-jest": "^24.1.0"
  },
  "dependencies": {}
}